<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RPGToDoList" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.RPGToDoList" parent="Base.Theme.RPGToDoList">
        <item name="android:windowSplashScreenAnimatedIcon" tools:ignore="NewApi">@drawable/logo</item> <!-- Your custom splash screen logo -->
        <item name="android:windowSplashScreenBackground" tools:ignore="NewApi">@android:color/black</item> <!-- Background color -->
    </style>
</resources>